<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Marvella Business Manager" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Marvella Business Manager">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-gray-800">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-20">
          <div class="flex items-center">
            <div class="shrink-0">
              <% if logged_in? %>
                <% if current_employee.organization_owner? %>
                  <%= link_to current_employee.organization.name, root_path, class: "text-white font-bold text-xl hover:text-gray-200" %>
                <% else %>
                  <%= link_to current_business.name, root_path, class: "text-white font-bold text-xl hover:text-gray-200" %>
                <% end %>
              <% else %>
                <span class="text-white font-bold text-xl">MBM</span>
              <% end %>
            </div>
            <% if logged_in? %>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Organizations", organizations_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Businesses", businesses_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Employees", employees_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <%= link_to "Clients", clients_path, class: "text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            <% end %>
          </div>
          <div class="flex items-center">
            <% if logged_in? %>
              <span class="text-gray-300 text-sm mr-4">
                <%= current_employee.name %> 
                <% if current_employee.organization_owner? %>
                  <span class="text-xs text-purple-400">(Owner)</span>
                <% elsif current_business %>
                  <span class="text-xs text-gray-400">(<%= current_employee.emp_role(current_business)&.role&.titleize || 'Staff' %>)</span>
                <% end %>
              </span>
              <%= button_to "Log Out", logout_path, method: :delete, 
                  class: "bg-red-600 hover:bg-red-700 text-white px-3 py-2 my-rounded-md text-sm font-medium" %>
            <% else %>
              <%= link_to "Log In", login_path, 
                  class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if flash[:notice] %>
      <div class="bg-green-50 border-l-4 border-green-400 p-4 mb-4">
        <p class="text-green-700"><%= flash[:notice] %></p>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-4">
        <p class="text-red-700"><%= flash[:alert] %></p>
      </div>
    <% end %>

    <main class="container mx-auto mt-8 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
